{
    
    "version":1,
    "build": {
        "env": {
          "YOUR_ENV_VARIABLE": "some-value"
        },
        "command": "npm run build",
        "functions": "api/**/*.js"
      },
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/"
        }
    ]
}